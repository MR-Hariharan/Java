package date0408.attendancerecord;
import java.util.*;
public class Mainfunction {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        AttendanceEmployee[] record = new AttendanceEmployee[2];
        for(int i = 0; i < record.length; i++){
            System.out.println("             enter the employee details "+(i + 1));
            System.out.print("enter the name:");
            String name = sc.nextLine();
            System.out.print("enter the id:");
            int id = sc.nextInt();
            sc.nextLine();
            System.out.print("enter the dept:");
            String dept = sc.nextLine();
            record[i] = new AttendanceEmployee(name, id, dept);
            System.out.println("enter the 7 values of 0 or 1:");
            int[] Attendance = new int[7];
            for(int j = 0; j < 7; j++){
                Attendance[j] = sc.nextInt();
            }
            sc.nextLine();
            record[i].markAttendance(Attendance);
        }
        for(AttendanceEmployee s : record){
            s.showDetails();
            s.displayAttendance();
        }
        Employee.showEmployeeCount();
        sc.close();






































































































































































































































































































































































































































































































































































































































    }
}
